# This file should contain packages which are required by package builds often
# enough to warrant download caching.
# It should not contain your own packages, to avoid circular dependencies
# (chicken and egg).

java-devel javapackages-tools
dia doxygen graphviz graphviz-gd
libssh-devel libssh
txt2man

