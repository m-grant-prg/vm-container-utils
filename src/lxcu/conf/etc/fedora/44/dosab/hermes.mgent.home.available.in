# If the server is avalable perform all the following commands.

# Add my RPM repository on that server.
cp /etc/dosab/hermes-beta.repo /etc/yum.repos.d
cp /etc/dosab/hermes-stable.repo /etc/yum.repos.d

